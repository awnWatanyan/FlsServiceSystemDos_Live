<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlsServiceSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item> <!-- Ensure this is false -->
        <item name="android:windowSoftInputMode">adjustResize</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FlsServiceSystem" parent="Base.Theme.FlsServiceSystem" />

    <style name="Theme.FlsServiceSystem.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FlsServiceSystem.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FlsServiceSystem.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.Deeplink2" parent="Theme.AppCompat.Light.NoActionBar" />

</resources>